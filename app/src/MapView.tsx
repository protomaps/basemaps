import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css'

// Renderer: (openlayers | maplibre gl)
// Theme : (light | dark | dataviz_light | grayscale | dark)
// Language : (en)

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <span>map view</span>
  </React.StrictMode>,
)
